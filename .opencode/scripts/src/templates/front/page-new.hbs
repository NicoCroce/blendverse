import { Page } from '@app/Application';

export const {{Entity}}NewPage = () => {
  return (
    <Page title="Nuevo {{Entity}}">
      {/* The agent must implement the feature-specific React Hook Form here. */}
    </Page>
  );
};
