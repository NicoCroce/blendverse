{
  "description": "Bloquea comandos de terminal destructivos e irreversibles antes de que el agente los ejecute.",
  "hooks": [
    {
      "event": "PreToolUse",
      "toolNames": ["runCommand"],
      "blockIfMatch": {
        "patterns": [
          "rm -rf",
          "git push --force",
          "git reset --hard",
          "DROP TABLE",
          "DROP DATABASE",
          "truncate",
          "git clean -fd",
          "npx prisma migrate reset"
        ],
        "message": "⛔ Comando destructivo bloqueado. Requiere confirmación explícita del usuario antes de ejecutar."
      }
    }
  ]
}
