import { {{EntityName}} } from '../{{EntityName}}.entity';
import { I{{EntityName}}Repository } from '../{{EntityName}}.repository';
import { I{{EntityName}}Filter } from '../{{EntityName}}.interfaces';

export class Get{{EntityName}}sUseCase {
  constructor(private readonly {{entityName}}Repository: I{{EntityName}}Repository) {}

  async execute(filters?: I{{EntityName}}Filter): Promise<{{EntityName}}[]> {
    const {{entityName}}s = await this.{{entityName}}Repository.get{{EntityName}}s(filters);
    return {{entityName}}s;
  }
}
